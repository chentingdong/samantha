const enum ItemOrigin{
  Catalog,
  Made,
  Received
}

const enum EditMode{
  Create,
  Edit
}

const enum MutationType {
  Create,
  Connect,
  Delete
}

export { ItemOrigin, EditMode, MutationType }