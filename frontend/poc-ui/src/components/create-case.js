import React from 'react'

/**
* @author tchen@bellhop.io
* @function CreateCaseForm
**/

const CreateCaseForm = (props) => {

  return(
    <form onSubmit={e => e.preventDefault()}>
      <p>This is only used in </p>
    </form>
  )
}

export default CreateCaseForm