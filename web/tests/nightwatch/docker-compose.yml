version: '2'
services:
  chrome:
    image: selenium/node-chrome:3.14.0
    volumes:
      - /dev/shm:/dev/shm
    depends_on:
      - hub
    environment:
      - HUB_HOST=hub
      - HUB_PORT=4444
  hub:
    image: selenium/hub:3.14.0
    ports:
      - "2077:4444"