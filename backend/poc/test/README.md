# function level unit testings

* create a test for an existing function, exp., createTaskDefinition
```bash
sls create test -f createTaskDefinition
```
* invoke test scripts from cli
```bash
sls invoke test -f createTaskDefinition
```
* debugging with vscode

see configuration in launch.json.
